name: job
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello this is build"
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello this is test"
  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello this is deploy"
      

